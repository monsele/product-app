# 0065 creative-design snapshot revision identity

This additive constraint correction permits an identical resolved design manifest
to be applied to a later immutable lesson-spec revision. It neither changes nor
rewrites existing snapshot rows. The replacement uniqueness rule keeps retries
idempotent for the same tenant, lesson, revision, and manifest hash.
